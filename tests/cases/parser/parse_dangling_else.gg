// braces are mandatory so "dangling else" is unambiguous: else belongs to the if written right before it
fun f() {
  if (a) { if (b) { x = 1; } } else { x = 2; }
  if (a) { if (b) { x = 1; } else { x = 2; } }
}
