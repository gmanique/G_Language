// a variable declared in a loop body is declared once per iteration, not redefined
fun main() i32 {
  i32 i = 0;
  i32 acc = 0;
  while (i < 3) {
    i32 sq = i * i;
    acc = acc + sq;
    i = i + 1;
  }
  return (acc);
}
