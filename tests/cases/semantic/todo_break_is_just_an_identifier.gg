// CURRENT gap: "break" parses as an identifier expression; it must be rejected (unknown variable) until it becomes a keyword (semantic analysis should reject this)
fun main() i32 {
  while (1) {
    break;
  }
  return (0);
}
